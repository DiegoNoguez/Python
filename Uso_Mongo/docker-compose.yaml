services:
  mongodb:
    image: mongo:latest
    container_name: mongodb_1
    ports:
      - "27017:27017"
    environment:
       MONGO_INITDB_ROOT_USERNAME: root
       MONGO_INITDB_ROOT_PASSWORD: 1234
    volumes:
      - ./mongo:/data/db
